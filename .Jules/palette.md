
## 2024-05-18 - Added tooltip to Settings IconButton
**Learning:** Icon-only buttons in Flutter lack visual hover context and semantic accessibility labels without explicit configuration. The `tooltip` property natively addresses both issues.
**Action:** Always provide a descriptive `tooltip` when implementing `IconButton` widgets without explicit text labels to ensure accessibility and intuitive UX.
