## $(date +%Y-%m-%d) - Add Tooltips to IconButtons
**Learning:** In Flutter, `IconButton` widgets without tooltips can be inaccessible to screen reader users and ambiguous to sighted users. Adding a `tooltip` property natively provides both hover text and semantic labels.
**Action:** Always include the `tooltip` property on `IconButton` widgets unless there's a specific reason not to.
