## 2025-02-09 - Added Tooltip to Settings Button
**Learning:** Tooltips natively serve dual purposes in Flutter: visual hover context and semantic labels for screen readers. Icon-only buttons should almost always have one.
**Action:** Always verify if `IconButton` elements have a `tooltip` assigned.
