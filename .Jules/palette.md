## 2026-06-12 - Add Tooltips to IconButtons
**Learning:** Flutters `IconButton` native `tooltip` property perfectly balances visual feedback and screen-reader accessibility for icon-only buttons without requiring custom ARIA-like wrappers.
**Action:** Use the `tooltip` property whenever an `IconButton` lacks text to provide both a hover context and a semantic label.
