## 2026-04-15 - Added Tooltip to Icon-Only Button
**Learning:** Flutter's `IconButton` requires a `tooltip` property for accessibility purposes, acting as both visual hover context for sighted users and a semantic ARIA-equivalent label for screen readers.
**Action:** When working on Flutter UI, ensure all icon-only buttons (`IconButton`) have a descriptive `tooltip` string to comply with UX/accessibility guidelines.
