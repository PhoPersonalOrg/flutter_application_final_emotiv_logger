## 2026-05-24 - Added Tooltip to Settings IconButton
**Learning:** In Flutter, adding a `tooltip` to an `IconButton` natively serves dual purposes: providing visual hover context and supplying the semantic label for screen readers. This is an easy way to improve accessibility for icon-only buttons.
**Action:** Always add a `tooltip` property to `IconButton` widgets that lack text labels.
