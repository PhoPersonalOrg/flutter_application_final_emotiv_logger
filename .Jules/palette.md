## 2024-05-24 - Flutter IconButton Accessibility
**Learning:** In Flutter, the `tooltip` property natively serves dual purposes: providing visual hover context and supplying the semantic label for screen readers. It is an essential property for icon-only buttons.
**Action:** Always add a `tooltip` property when creating `IconButton` widgets or other icon-only interactive elements in Flutter.
