## 2026-06-26 - [Added Tooltip to Settings IconButton]
**Learning:** In Flutter, adding a `tooltip` property to an `IconButton` is a crucial micro-UX improvement because it serves a dual purpose: providing visual hover text and semantic accessibility labeling for screen readers.
**Action:** Always check icon-only buttons for missing `tooltip` or `semanticsLabel` properties to ensure they are fully accessible.
