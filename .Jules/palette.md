## 2024-05-18 - Add tooltip to IconButton
**Learning:** In Flutter, the `tooltip` property natively serves dual purposes: providing visual hover context and supplying the semantic label for screen readers. Using `tooltip` on `IconButton` is the standard way to add ARIA equivalents.
**Action:** Always add a `tooltip` property to icon-only buttons for accessibility.
