## 2024-05-18 - Added Tooltip to Settings IconButton
**Learning:** Adding a `tooltip` to an `IconButton` in Flutter provides both visual hover context for web/desktop users and an accessible semantic label for screen readers on mobile devices, solving two UX issues with one simple property.
**Action:** Always check `IconButton`s for the `tooltip` property to ensure icon-only buttons are accessible.
