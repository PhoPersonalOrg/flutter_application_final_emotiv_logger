## 2025-02-12 - App Bar Tooltips
**Learning:** Adding a `tooltip` to `IconButton` elements like the App Bar settings button is a quick and effective way to provide textual context for screen readers and visual hover text.
**Action:** Always verify icon-only buttons have a `tooltip` attribute applied.
